services:
  nginx:
    image: nginx:1.29-alpine-slim
    ports:
      - "8080:80"
      - "9000:9000"
      - "9001:9001"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
  api:
    build: .
    image: motochatz-be:latest
    expose:
      - "3000"
    environment:
      NODE_ENV: ${NODE_ENV}
      DATABASE_URI: "mongodb://db:27017/motochatz?replicaSet=rs0"
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN}
      S3_REGION: "us-east-1"
      S3_ENDPOINT: "http://s3:9000"
      S3_PUBLIC_ENDPOINT: ${S3_PUBLIC_ENDPOINT}
      S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID}
      S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY}
      S3_BUCKET_NAME: ${S3_BUCKET_NAME}
      S3_PATH_STYLE: "PATH"
      AI_SERVER_URL: "http://mock-ai:3001"
      AI_API_KEY: ${AI_API_KEY}
    depends_on:
      db:
        condition: service_healthy
      s3:
        condition: service_healthy
      mock-ai:
        condition: service_started
  db:
    image: mongo:8.0-noble
    restart: always
    volumes:
      - db-data:/data/db
    command: ["--replSet", "rs0", "--bind_ip_all", "--port", "27017"]
    healthcheck:
      test: >
        mongosh --port 27017 --quiet --eval "
          try {
            if (rs.status().ok) {
              quit(0);
            }
          } catch (e) {
            rs.initiate({
              _id: 'rs0',
              members: [{ _id: 0, host: 'db:27017' }]
            });

            quit(1);
          }
        "
      interval: 10s
      timeout: 10s
      retries: 5
  s3:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    restart: always
    volumes:
      - s3-data:/data
    command: server /data/s3 --address ":9000" --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY_ID}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_ACCESS_KEY}
      MINIO_BROWSER_REDIRECT_URL: "http://localhost:9001"
    expose:
      - "9000"
      - "9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
  mock-ai:
    build:
      context: ../mocks/mock-ai/
    image: mock-ai:latest
    expose:
      - "3001"
    environment:
      AI_API_KEY: ${AI_API_KEY}
volumes:
  db-data:
  s3-data:

